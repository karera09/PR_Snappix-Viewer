"""Snappix Viewer — the viewer application package.

Launched via ``SnappixViewer.exe`` (frozen) or ``python -m snappix`` (dev).
Depends only on ``snappix.common`` outside this package; performs no
network access.
"""
